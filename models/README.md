# Models Directory

This directory contains pre-trained model files.

## Files

- `yolo11n.pt` - YOLOv11 nano model (base model for training)

## Trained Models

Trained models are saved to `../outputs/models/` after training.
